# Cloud Migration Architecture

## Current Architecture (Local SQLite)

```
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│              Genesis AI Hub Application (Local)                │
│                                                               │
└───────────────┬───────────────────────────────────────────────┘
                │
                │ All storage is LOCAL on your Mac
                │
    ┌───────────┴────────────┬──────────────┬──────────────┐
    │                        │              │              │
    ▼                        ▼              ▼              ▼
┌─────────┐            ┌──────────┐   ┌──────────┐   ┌─────────┐
│ SQLite  │            │ ChromaDB │   │ uploads/ │   │ cache/  │
│         │            │          │   │          │   │         │
│webui.db │            │ .sqlite3 │   │  files   │   │  temp   │
│ 260KB   │            │  164KB   │   │    0B    │   │   0B    │
└─────────┘            └──────────┘   └──────────┘   └─────────┘

  Users,                 Vector           Uploaded      Temporary
  Chats,               Embeddings          Files         Cache
 Messages,
  Configs
```

**Characteristics:**
- ✅ Simple setup
- ✅ No external dependencies
- ❌ Limited scalability
- ❌ Single instance only
- ❌ Manual backups
- ❌ File corruption risk

---

## Target Architecture (Google Cloud)

```
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│          Genesis AI Hub Application (Anywhere)                 │
│                                                               │
│              Can run: Locally, Cloud Run, GCE, GKE            │
│                                                               │
└───────────┬───────────────────────────────────────────────────┘
            │
            │ All storage is CLOUD-BASED
            │
    ┌───────┴──────────┬─────────────────┬────────────┐
    │                  │                 │            │
    ▼                  ▼                 ▼            ▼
┌─────────────┐   ┌──────────────┐  ┌─────────┐  ┌──────────┐
│             │   │              │  │ Google  │  │  cache/  │
│  Cloud SQL  │   │   PgVector   │  │ Cloud   │  │          │
│             │   │              │  │ Storage │  │   temp   │
│ PostgreSQL  │   │ (in same DB) │  │         │  │  (local) │
│             │   │              │  │  (GCS)  │  │          │
│  Scalable   │   │   Scalable   │  │Unlimited│  │          │
└─────────────┘   └──────────────┘  └─────────┘  └──────────┘
      │                  │                │
      │                  │                │
      ▼                  ▼                ▼
  
  Users,              Vector         Uploaded      Temporary
  Chats,            Embeddings         Files         Cache
 Messages,                                          (Still
  Configs                                           Local)
```

**Characteristics:**
- ✅ Highly scalable (1 to millions of users)
- ✅ High availability (99.95% SLA)
- ✅ Automatic backups (daily + point-in-time)
- ✅ Multi-instance support
- ✅ Unlimited storage
- ✅ Enterprise security
- ✅ Geographic distribution
- ✅ Professional monitoring

---

## Data Flow Comparison

### Before: Local SQLite

```
User Request
     │
     ▼
┌──────────────┐
│ Application  │
└──────┬───────┘
       │
       ├─► Read/Write webui.db (local file)
       │   └─ LOCK file during write (blocks)
       │
       ├─► Query chroma.sqlite3 (local file)
       │   └─ Embedding search
       │
       └─► Access uploads/ folder (local)
           └─ File operations

⏱️  Latency: <1ms (local disk)
📊 Concurrency: Limited (file locks)
🔒 Security: File permissions only
💾 Backup: Manual
```

### After: Google Cloud

```
User Request
     │
     ▼
┌──────────────┐
│ Application  │
└──────┬───────┘
       │
       ├─► Cloud SQL PostgreSQL (via Cloud SQL Proxy)
       │   ├─ Encrypted connection
       │   ├─ Connection pooling
       │   ├─ Automatic failover
       │   └─ Automatic backups
       │
       ├─► PgVector (same PostgreSQL instance)
       │   ├─ Optimized vector index
       │   └─ Concurrent queries
       │
       └─► Google Cloud Storage (via API)
           ├─ CDN integration
           ├─ Versioning enabled
           └─ Lifecycle policies

⏱️  Latency: 5-50ms (network + optimized)
📊 Concurrency: Unlimited (MVCC)
🔒 Security: IAM, encryption, audit logs
💾 Backup: Automatic (daily + PITR)
```

---

## Migration Path

```
┌─────────────────────────────────────────────────────────────┐
│                    Migration Process                         │
└─────────────────────────────────────────────────────────────┘

Step 1: Setup Google Cloud (5 minutes)
┌────────────────────────────────────────┐
│  ./setup_google_cloud.sh               │
└──────────────┬─────────────────────────┘
               │
               ├─► Create Cloud SQL Instance
               ├─► Create GCS Bucket
               ├─► Create Service Account
               ├─► Download Cloud SQL Proxy
               └─► Generate .env config
               
Step 2: Enable PgVector (1 minute)
┌────────────────────────────────────────┐
│  CREATE EXTENSION vector;              │
└────────────────────────────────────────┘

Step 3: Start Cloud SQL Proxy
┌────────────────────────────────────────┐
│  ./start_cloud_sql_proxy.sh            │
│  (keeps running in background)         │
└────────────────────────────────────────┘

Step 4: Migrate Data (5 minutes)
┌────────────────────────────────────────┐
│  ./migrate_to_postgres.sh              │
└──────────────┬─────────────────────────┘
               │
               ├─► Backup SQLite
               ├─► Export to SQL
               ├─► Convert syntax
               ├─► Import to PostgreSQL
               ├─► Verify data
               └─► Generate report

Step 5: Update Configuration (1 minute)
┌────────────────────────────────────────┐
│  cp .env.cloud.generated .env          │
└────────────────────────────────────────┘

Step 6: Start Application (1 minute)
┌────────────────────────────────────────┐
│  cd front/backend                      │
│  ./start.sh                            │
└────────────────────────────────────────┘

Step 7: Verify & Test (3 minutes)
┌────────────────────────────────────────┐
│  • Login                               │
│  • Check chats                         │
│  • Upload file                         │
│  • Test search                         │
└────────────────────────────────────────┘

✅ Total Time: ~15 minutes
```

---

## Network Architecture

### Development Setup (Current)

```
┌─────────────────────┐
│   Your Mac          │
│                     │
│  ┌──────────────┐   │
│  │ Application  │   │
│  │ :8080        │   │
│  └──────┬───────┘   │
│         │           │
│         ▼           │
│  ┌──────────────┐   │
│  │ Cloud SQL    │◄──┼── Cloud SQL Proxy
│  │ Proxy        │   │   (localhost:5432)
│  │ :5432        │   │
│  └──────┬───────┘   │
└─────────┼───────────┘
          │
          │ Secure Tunnel
          │ (encrypted)
          │
    ┌─────▼──────────────────────────┐
    │    Google Cloud                │
    │                                │
    │  ┌──────────────────────────┐  │
    │  │  Cloud SQL PostgreSQL    │  │
    │  │  (Private IP)            │  │
    │  └──────────────────────────┘  │
    │                                │
    │  ┌──────────────────────────┐  │
    │  │  Cloud Storage (GCS)     │  │
    │  │  (Public with IAM)       │  │
    │  └──────────────────────────┘  │
    └────────────────────────────────┘
```

### Production Setup (Future)

```
    ┌──────────────────┐
    │   Users          │
    │   (Browser)      │
    └────────┬─────────┘
             │
             │ HTTPS
             │
    ┌────────▼─────────────────────────┐
    │    Google Cloud                  │
    │                                  │
    │  ┌───────────────────────────┐   │
    │  │  Cloud Load Balancer      │   │
    │  │  + SSL Certificate        │   │
    │  └─────────┬─────────────────┘   │
    │            │                     │
    │            ▼                     │
    │  ┌───────────────────────────┐   │
    │  │  Cloud Run (Serverless)   │   │
    │  │  • Auto-scaling           │   │
    │  │  • Multi-region           │   │
    │  │  • Rolling updates        │   │
    │  └──┬────────────────────┬───┘   │
    │     │                    │       │
    │     ▼                    ▼       │
    │  ┌─────────────┐   ┌──────────┐  │
    │  │  Cloud SQL  │   │   GCS    │  │
    │  │  (HA setup) │   │(Unlimited)  │
    │  └─────────────┘   └──────────┘  │
    └──────────────────────────────────┘
```

---

## Cost Evolution

```
Development:     $10/month    ████
Small Prod:      $60/month    ████████████████████
Medium Prod:    $130/month    ████████████████████████████████████
Enterprise:     $500+/month   ████████████████████████████████████████████████████████████

                              ↑
                              Scales with usage
```

**Cost Breakdown:**
- 90% = Compute (Cloud SQL tier)
- 8% = Storage (data volume)
- 2% = Operations (API calls)

**Optimization Tips:**
- Start with db-f1-micro ($7/mo)
- Monitor Cloud Console metrics
- Upgrade tier only when needed
- Use committed use discounts (production)
- Enable storage lifecycle policies

---

## Security Model

### Before (SQLite)

```
┌──────────────────┐
│  File System     │
│  Permissions     │
└──────────────────┘
       │
       ▼
┌──────────────────┐
│  webui.db        │
│  chmod 600       │
└──────────────────┘
```

- Single layer security
- File permissions only
- No audit trail
- Manual backups

### After (Cloud SQL + GCS)

```
┌───────────────────────────────────────┐
│  Layer 1: Google Cloud IAM           │
│  • Service Account roles              │
│  • Fine-grained permissions           │
└───────────────┬───────────────────────┘
                │
┌───────────────▼───────────────────────┐
│  Layer 2: Network Security            │
│  • Private IP (VPC)                   │
│  • Cloud SQL Proxy (encrypted)        │
└───────────────┬───────────────────────┘
                │
┌───────────────▼───────────────────────┐
│  Layer 3: Database Security           │
│  • User authentication                │
│  • SSL/TLS required                   │
│  • Audit logging                      │
└───────────────┬───────────────────────┘
                │
┌───────────────▼───────────────────────┐
│  Layer 4: Storage Security            │
│  • Encryption at rest (AES-256)       │
│  • Encryption in transit (TLS 1.3)    │
│  • Object versioning                  │
└───────────────┬───────────────────────┘
                │
┌───────────────▼───────────────────────┐
│  Layer 5: Backup & Recovery           │
│  • Automatic daily backups            │
│  • Point-in-time recovery             │
│  • Cross-region replication           │
└───────────────────────────────────────┘
```

---

## Rollback Safety

```
Migration Attempt
     │
     ├─► ✅ Automated Backup Created
     │      (Before any changes)
     │
     ├─► ✅ PostgreSQL Import
     │      (New database, old untouched)
     │
     ├─► ❓ Test Migration
     │
     ├─► ✅ Success? → Use new database
     │
     └─► ❌ Failed? → Rollback in 2 minutes
                      │
                      ├─► Stop application
                      ├─► Restore .env
                      ├─► Copy backup
                      └─► Restart app
                      
Your original SQLite database is NEVER modified!
```

---

**Next Steps:** Start with `./setup_google_cloud.sh`
